# Roadmap — v1

**Doc de trabajo descartable.** Se borra al cerrar la v1. No es registro histórico ni fuente de verdad: el estado de implementación vive en `docs/features.md` y el alcance en `docs/requirements.md` §2.

---

## Estado

v1 **en definición**. La documentación funcional, de datos y técnica está cerrada; no hay features implementadas todavía.

**Sin pendientes bloqueantes.**

## Notas para la implementación

Nada de esto bloquea el cierre de la documentación. Son specs visuales que `design` produce cuando se implemente la pantalla correspondiente, por el paso 4.6 del flujo del orquestador (design → frontend por spec).

- **Distinción visual entre un requisito fijo de equipo y un grupo de alternativas** ("mancuernas Y banco" vs. "mancuernas O kettlebell"), en la edición y en el listado de ejercicios. Lo funcional ya está fijado en `docs/screens.md` §4.
